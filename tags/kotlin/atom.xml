<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
	<title>Anurag Soni - Kotlin</title>
	<subtitle>Anurag Soni&#x27;s personal blog</subtitle>
	<link href="https://anuragsoni.github.io/tags/kotlin/atom.xml" rel="self" type="application/atom+xml"/>
  <link href="https://anuragsoni.github.io"/>
	<generator uri="https://www.getzola.org/">Zola</generator>
	<updated>2022-09-15T00:00:00+00:00</updated>
	<id>https://anuragsoni.github.io/tags/kotlin/atom.xml</id>
	<entry xml:lang="en">
		<title>Using Kotlin Coroutines with Vert.x</title>
		<published>2022-09-15T00:00:00+00:00</published>
		<updated>2022-09-15T00:00:00+00:00</updated>
		<link rel="alternate" href="https://anuragsoni.github.io/post/vertx-kotlin/" type="text/html"/>
		<id>https://anuragsoni.github.io/post/vertx-kotlin/</id>
		<content type="html">&lt;p&gt;&lt;a href=&quot;https:&#x2F;&#x2F;vertx.io&quot;&gt;Vert.x&lt;&#x2F;a&gt; is a toolkit for writing asynchronous and reactive applications on the JVM. The Vert.x ecosystem has high-quality libraries providing support for web services, database drivers, authentication, and more.&lt;&#x2F;p&gt;
&lt;p&gt;I&#x27;ve recently started to use Vert.x, but I chose to use it with Kotlin instead of Java. I&#x27;m still in my first week of using Kotlin, but I&#x27;m finding the language very pleasant so far. This post will talk about Vert.x Web which is a library that builds on top of the core Vert.x library to provide everything one would need to write an HTTP&#x2F;HTTP2 service. It is similar in spirit to Sinatra from the Ruby world, or Flask from Python.&lt;&#x2F;p&gt;
&lt;p&gt;Vert.x is a fully concurrent&#x2F;asynchronous toolkit and it offers a few different units of abstractions to help make this approachable. When using the library with Java, callbacks, promises and futures are available and should feel very familiar to anyone used to Javascript, Async&#x2F;Lwt from OCaml, etc. While promises and futures work well, I am using Vert.x with Kotlin so I wanted to use coroutines as the unit of concurrency, and benefit from being able to write my async operations in direct-style. This is where I ran into some gaps in the Vert.x HTTP library.&lt;&#x2F;p&gt;
&lt;p&gt;Vert.x&#x27;s Kotlin support is in the form of a library that implements some functions that allow one to &lt;code&gt;await&lt;&#x2F;code&gt; a Vert.x Future from within a kotlin coroutine. Beyond this, Kotlin users access the same Vert.x APIs that are used from Java that is designed around promises and futures. When writing client code, it was easy to use the &lt;code&gt;await&lt;&#x2F;code&gt; implementations from the vertx-kotlin support library from within a kotlin coroutine, but it took me a while to figure out a satisfactory solution that allows defining HTTP handlers that are kotlin coroutines, thus allowing the handlers to consume other Kotlin APIs that work with coroutines instead of vert.x promises and futures.&lt;&#x2F;p&gt;
&lt;p&gt;As an example, creating a router using the default API from Vert.x Web looks like this:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;kotlin&quot; class=&quot;language-kotlin z-code&quot;&gt;&lt;code class=&quot;language-kotlin&quot; data-lang=&quot;kotlin&quot;&gt;&lt;span class=&quot;z-source z-Kotlin&quot;&gt;&lt;span class=&quot;z-keyword z-other z-kotlin&quot;&gt;val&lt;&#x2F;span&gt; &lt;span class=&quot;z-entity z-name z-variable z-kotlin&quot;&gt;router&lt;&#x2F;span&gt; &lt;span class=&quot;z-keyword z-operator z-assignment z-kotlin&quot;&gt;=&lt;&#x2F;span&gt; Router&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;router(vertx)&lt;span class=&quot;z-punctuation z-terminator z-kotlin&quot;&gt;;&lt;&#x2F;span&gt;

router&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;route(&lt;span class=&quot;z-string z-quoted z-double z-kotlin&quot;&gt;&lt;span class=&quot;z-punctuation z-definition z-string z-begin z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&#x2F;hello&lt;span class=&quot;z-punctuation z-definition z-string z-end z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;)&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;handler { requestContext &lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;-&amp;gt;&lt;&#x2F;span&gt;
    requestContext&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;response()&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;send(&lt;span class=&quot;z-string z-quoted z-double z-kotlin&quot;&gt;&lt;span class=&quot;z-punctuation z-definition z-string z-begin z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;Hello World&lt;span class=&quot;z-punctuation z-definition z-string z-end z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;)
}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;For simple handlers, and if the APIs that are needed within the handler use promises&#x2F;callbacks, then this works just fine. While promises work fine, the callback style APIs can be a little difficult to read, and one big perk of using Kotlin is its support for coroutines that help write direct-style asynchronous code. It is fairly common to see coroutines used in Kotlin libraries, so it was important for me to find a way to write HTTP handlers that can work with coroutines seamlessly.&lt;&#x2F;p&gt;
&lt;p&gt;We will simulate some async work using a simple suspendable function that sleeps for a duration before responding.&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;kotlin&quot; class=&quot;language-kotlin z-code&quot;&gt;&lt;code class=&quot;language-kotlin&quot; data-lang=&quot;kotlin&quot;&gt;&lt;span class=&quot;z-source z-Kotlin&quot;&gt;&lt;span class=&quot;z-storage z-modifier z-kotlin&quot;&gt;suspend&lt;&#x2F;span&gt; &lt;span class=&quot;z-keyword z-other z-kotlin&quot;&gt;fun&lt;&#x2F;span&gt; &lt;span class=&quot;z-entity z-name z-function z-kotlin&quot;&gt;coroutineDemo&lt;&#x2F;span&gt;()&lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;:&lt;&#x2F;span&gt; &lt;span class=&quot;z-storage z-type z-buildin z-kotlin&quot;&gt;String&lt;&#x2F;span&gt; {
    delay(&lt;span class=&quot;z-constant z-numeric z-kotlin&quot;&gt;3000&lt;&#x2F;span&gt;)
    &lt;span class=&quot;z-keyword z-control z-kotlin&quot;&gt;return&lt;&#x2F;span&gt; &lt;span class=&quot;z-string z-quoted z-double z-kotlin&quot;&gt;&lt;span class=&quot;z-punctuation z-definition z-string z-begin z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;Hello World&lt;span class=&quot;z-punctuation z-definition z-string z-end z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;
}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Trying to call &lt;code&gt;coroutineDemo()&lt;&#x2F;code&gt; within the vert.x route handler will result in an error saying that &lt;code&gt;Suspension functions can be called only within the coroutine body&lt;&#x2F;code&gt;. What we need is a way to define a new kind of route handler that allows users to provide a suspendable function as a handler and transforms it into a callback-based handler that Vert.x Web understands. Lucky for us Kotlin comes with a pretty neat way to extend third-party libraries, called &lt;a href=&quot;https:&#x2F;&#x2F;kotlinlang.org&#x2F;docs&#x2F;extensions.html&quot;&gt;Extensions&lt;&#x2F;a&gt;. With extensions, we can extend the Vert.x Route interface to add a new handler type that works with coroutines.&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;kotlin&quot; class=&quot;language-kotlin z-code&quot;&gt;&lt;code class=&quot;language-kotlin&quot; data-lang=&quot;kotlin&quot;&gt;&lt;span class=&quot;z-source z-Kotlin&quot;&gt;&lt;span class=&quot;z-keyword z-other z-kotlin&quot;&gt;fun&lt;&#x2F;span&gt; Route.&lt;span class=&quot;z-entity z-name z-function z-kotlin&quot;&gt;coroutineHandler&lt;&#x2F;span&gt;(
    &lt;span class=&quot;z-variable z-parameter z-function z-kotlin&quot;&gt;coroutineContext&lt;&#x2F;span&gt;&lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;:&lt;&#x2F;span&gt; CoroutineContext,
    &lt;span class=&quot;z-variable z-parameter z-function z-kotlin&quot;&gt;userHandler&lt;&#x2F;span&gt;&lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;:&lt;&#x2F;span&gt; suspend (RoutingContext) &lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;-&amp;gt;&lt;&#x2F;span&gt; &lt;span class=&quot;z-storage z-type z-buildin z-kotlin&quot;&gt;Unit&lt;&#x2F;span&gt;
) &lt;span class=&quot;z-keyword z-operator z-assignment z-kotlin&quot;&gt;=&lt;&#x2F;span&gt; handler { routingContext &lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;-&amp;gt;&lt;&#x2F;span&gt;
    CoroutineScope(coroutineContext)&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;launch {
        &lt;span class=&quot;z-keyword z-control z-catch-exception z-kotlin&quot;&gt;try&lt;&#x2F;span&gt; {
            userHandler(routingContext)
        } &lt;span class=&quot;z-keyword z-control z-catch-exception z-kotlin&quot;&gt;catch&lt;&#x2F;span&gt; (exception&lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;:&lt;&#x2F;span&gt; Exception) {
            routingContext&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;fail(exception)
        }
    }
}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;This is a short but fairly dense function. Starting at the top we can see:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;A &lt;a href=&quot;https:&#x2F;&#x2F;kotlinlang.org&#x2F;api&#x2F;latest&#x2F;jvm&#x2F;stdlib&#x2F;kotlin.coroutines&#x2F;-coroutine-context&#x2F;&quot;&gt;CoroutineContext&lt;&#x2F;a&gt; since every kotlin coroutine executes within a context, and we&#x27;d like to allow the user to forward the context set up by the vertx-kotlin library.&lt;&#x2F;li&gt;
&lt;li&gt;A &lt;code&gt;userHandler&lt;&#x2F;code&gt; that is similar to the default handler type &lt;code&gt;(RoutingContext -&amp;gt; Unit)&lt;&#x2F;code&gt;, but is a suspendable function instead.&lt;&#x2F;li&gt;
&lt;li&gt;We create a new Vert.x route handler, set up a new &lt;a href=&quot;https:&#x2F;&#x2F;kotlinlang.org&#x2F;api&#x2F;kotlinx.coroutines&#x2F;kotlinx-coroutines-core&#x2F;kotlinx.coroutines&#x2F;-coroutine-scope&#x2F;&quot;&gt;CoroutineScope&lt;&#x2F;a&gt; that is used to launch the suspendable handler.&lt;&#x2F;li&gt;
&lt;li&gt;We catch any unhandled exceptions and forward them to the vert.x router context so it can mark the request as a failure, call the vert.x error handler, and send an appropriate error response to the user.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;With this extension function in place, we can now add suspendable handlers to the vert.x router:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;kotlin&quot; class=&quot;language-kotlin z-code&quot;&gt;&lt;code class=&quot;language-kotlin&quot; data-lang=&quot;kotlin&quot;&gt;&lt;span class=&quot;z-source z-Kotlin&quot;&gt;&lt;span class=&quot;z-keyword z-other z-kotlin&quot;&gt;val&lt;&#x2F;span&gt; &lt;span class=&quot;z-entity z-name z-variable z-kotlin&quot;&gt;router&lt;&#x2F;span&gt; &lt;span class=&quot;z-keyword z-operator z-assignment z-kotlin&quot;&gt;=&lt;&#x2F;span&gt; Router&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;router(vertx)&lt;span class=&quot;z-punctuation z-terminator z-kotlin&quot;&gt;;&lt;&#x2F;span&gt;

router&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;route(&lt;span class=&quot;z-string z-quoted z-double z-kotlin&quot;&gt;&lt;span class=&quot;z-punctuation z-definition z-string z-begin z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&#x2F;hello&lt;span class=&quot;z-punctuation z-definition z-string z-end z-kotlin&quot;&gt;&amp;quot;&lt;&#x2F;span&gt;&lt;&#x2F;span&gt;)&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;coroutineHandler(coroutineContext) { routingContext &lt;span class=&quot;z-keyword z-operator z-declaration z-kotlin&quot;&gt;-&amp;gt;&lt;&#x2F;span&gt;
    &lt;span class=&quot;z-keyword z-other z-kotlin&quot;&gt;val&lt;&#x2F;span&gt; &lt;span class=&quot;z-entity z-name z-variable z-kotlin&quot;&gt;payload&lt;&#x2F;span&gt; &lt;span class=&quot;z-keyword z-operator z-assignment z-kotlin&quot;&gt;=&lt;&#x2F;span&gt; coroutineDemo()
    routingContext&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;response()&lt;span class=&quot;z-keyword z-operator z-dot z-kotlin&quot;&gt;.&lt;&#x2F;span&gt;send(payload)
}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The coroutineContext referenced in this snippet is the context available in a class that extends &lt;a href=&quot;https:&#x2F;&#x2F;vertx.io&#x2F;docs&#x2F;vertx-lang-kotlin-coroutines&#x2F;kotlin&#x2F;#_extending_coroutineverticle&quot;&gt;CoroutineVerticle&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;That is all for this post! This took a few tries for me to figure out as a new Kotlin user, and hopefully, some of you will find this useful! If you like this post or have feedback do let me know, either via &lt;a href=&quot;mailto:github@sonianurag.com&quot;&gt;email&lt;&#x2F;a&gt; or on &lt;a href=&quot;https:&#x2F;&#x2F;github.com&#x2F;anuragsoni&#x2F;anuragsoni.github.io&#x2F;discussions&quot;&gt;github&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;All the code in this post can be found &lt;a href=&quot;https:&#x2F;&#x2F;gist.github.com&#x2F;anuragsoni&#x2F;3680b9c30ba07ff16896490310c4fa7a&quot;&gt;here&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
</content>
	</entry>
</feed>
